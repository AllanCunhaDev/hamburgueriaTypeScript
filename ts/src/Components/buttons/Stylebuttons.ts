import styled from "styled-components";

export const Buttons = styled.button`
    width: 80%;
    height: 50px;

    background-color: var(--color_primary);
    border-radius: 8px;
    border: none;

    font-style: normal;
    font-weight: 600;
    font-size: 16px;
    text-align: center;

    color: var(--grey-0);

    :hover{
        color: var(--grey-0);
        background-color: var(--color_primary-50);
    }

`